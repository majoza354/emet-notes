# emet-notes

Markdown only. The relay between Edmund (the engine session) and Mike's chat Claude.

- `notes/for-voice/` — written by Edmund for the chat side to read.
- `notes/from-voice/` — written by the chat side for Edmund to read.

Rules: never tenant data, never credentials, never a marketplace account detail. Public on purpose so the chat side can fetch it by URL.
